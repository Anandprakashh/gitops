apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-anandcloud-tls
  namespace: argocd
spec:
  secretName: wildcard-anandcloud-tls
  issuerRef:
    name: letsencrypt-dns01
    kind: ClusterIssuer
  dnsNames:
    - "*.anandcloud.com"
    - "anandcloud.com"
