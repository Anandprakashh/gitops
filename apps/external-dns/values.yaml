provider: aws
registry: txt
txtOwnerId: "external-dns"
domainFilters:
  - gitops.anand.local

serviceAccount:
  create: false
  name: external-dns
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::889490400768:role/ExternalDNS-Role

policy: upsert-only
interval: 1m
