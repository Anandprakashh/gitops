apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-gitops
  namespace: my-app
spec:
  secretName: wildcard-gitops-tls
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
  dnsNames:
  - "*.gitops.anand.local"
